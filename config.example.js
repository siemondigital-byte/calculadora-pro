/* Copia este archivo como `config.js` y pon tus llaves de Supabase.
   La `anon key` es pública (segura para el navegador); la `service key` NUNCA va aquí.
   `config.js` está en .gitignore para no subir nada por error. */
window.CRD_CONFIG = {
  supabaseUrl: 'https://TU-PROYECTO.supabase.co',
  supabaseAnonKey: 'TU_ANON_KEY_PUBLICA'
};
